/* Esconde os detalhes internos de um objeto */


private     // só pode ser acessado dentro da class
public      // poder ser acessado de qualquer lugar
protected   // só poder ser acessado dentro do mesmo pacote



// getters e setters 

/* 

    getters -> usado para ler
    setters -> usado para atribuir as propriedades privadas 


    public String getNome() {
        return nome;
    }

    public void setNome(String nome) {
        if (nome != null && !nome.isEmpty()) {
            this.nome = nome;
        }
    }

*/


// 